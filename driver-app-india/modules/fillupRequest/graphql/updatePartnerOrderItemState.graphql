mutation updatePartnerOrderItemState(
  $id: uuid!
  $state: partner_order_item_state_enum!
) {
  update_partner_order_item_by_pk(
    pk_columns: {id: $id}
    _set: {state: $state}
  ) {
    id
    state
    created_at
  }
}
