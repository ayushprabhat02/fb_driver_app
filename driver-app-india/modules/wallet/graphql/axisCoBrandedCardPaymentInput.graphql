mutation axisBankPayment($object: axisBankPaymentInput!) {
  axisBankPayment(object: $object) {
    amount
    atom_token_id
    call_back_url
    transaction_id
    wallet_id
    wallet_transaction_id
    email
    phone_number
  }
}
